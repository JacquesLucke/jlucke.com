---
interface Props {
  label: string;
  id: string;
}

const { label, id } = Astro.props;
---

<button
  id={id}
  class="bg-surface-1 hover:bg-surface-2 cursor-pointer rounded px-2 py-1 font-sans text-sm transition"
  >{label}</button
>
